spring:
  application:
    name: system-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: edu-plus
      config:
        namespace: dev
        group: edu-plus
        file-extension: yaml
        refresh-enabled: true

  #profiles默认为dev
  profiles:
    active: dev
